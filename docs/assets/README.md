# Site assets

Images and diagrams **served with GitHub Pages** must live under `docs/` (this folder). Reference them from Markdown with site-relative paths, e.g. `![Diagram](assets/overview.svg)` from `docs/index.md`.

- Prefer **SVG** or **PNG** with concise filenames.
- Avoid large binaries when a link to an official source is enough.
